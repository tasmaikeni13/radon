\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{cite}
\usepackage{xcolor}
\usepackage{tcolorbox}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!70!black,
    citecolor=green!50!black,
    urlcolor=blue!80!black
}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\theoremstyle{remark}
\newtheorem{remark}{Remark}

\title{\textbf{RADON: Optimal Tomographic Probing for Neural Curvature\\via Coded Directional Projections}}

\author{
  \textbf{Tasma Ikeni}\thanks{Independent Research. Correspondence to: \texttt{tas.ken.rt25@dypatil.edu}.}
}
\date{September 2026}

\begin{document}

\maketitle

\begin{abstract}
Second-order optimization in deep learning is constrained by the cost of storing or factorizing the loss Hessian $H \in \mathbb{R}^{N \times N}$ ($N \sim 10^7 - 10^{11}$). Hessian-vector products (HVPs) expose the action of $H$ along chosen directions without materializing the matrix. Stochastic diagonal estimators can probe $H$ with independent random vectors; their off-diagonal cross-talk variance decreases as the number of probes grows.
In this work, we introduce \textbf{RADON} (\textbf{R}esidual-aware \textbf{A}ntithetic \textbf{D}ecoupled \textbf{O}rthogonal \textbf{N}ewton), a second-order optimization and curvature estimation framework that uses two algebraic principles for diagonal curvature estimation:
(1) \textbf{Compositional Split-Exact Curvature}: For composite losses $\ell \circ f$, the Hessian decomposes exactly as $H = S + R$, where $S = J_f^\top (\nabla^2 \ell) J_f$ is a positive semidefinite structural core for convex outer losses. Its diagonal is estimated with sampled-label gradients and no double backward. The residual $R = \mathcal{H}_f(\nabla \ell)$ is linear in the outer-loss gradient, so its ideal probe variance scales quadratically when that gradient is scaled with model derivatives fixed.
(2) \textbf{Coded Tomographic Probing}: By devoting all directional probes exclusively to the residual $R$, and assigning Sylvester-Hadamard codes via Latin-square coloring on parameter tensor grids, immediate row and column neighbor interference is cancelled ($\bar{C}_{ij} = 0$), yielding zero contribution from those pairs in the ideal residual-probe variance.
The repository includes Lean 4 formal proofs and small numerical checks. Large-scale pre-training, TPU throughput, and comparative optimizer performance remain to be measured.
\end{abstract}

\section{Introduction}

Modern deep learning is dominated by first-order adaptive gradient methods such as AdamW~\cite{loshchilov2018decoupled}, despite the notorious ill-conditioning and anisotropic curvature of deep loss landscapes. True second-order methods (e.g., Newton-Raphson, Natural Gradient) promise accelerated convergence and scale-invariance, but their exact execution requires storing and inverting the $N \times N$ loss Hessian $H = \nabla^2 \mathcal{L}(\theta)$, which is computationally impossible when $N \ge 10^8$.

Consequently, second-order optimization must operate under a strict measurement constraint: access to curvature is granted only via directional Hessian-vector products (HVPs)~\cite{martens2010deep}:
\begin{equation}
p_k = H v_k, \quad v_k \in \mathbb{R}^N, \quad k = 1, \dots, m, \quad m \ll N
\end{equation}
Each HVP is an $N$-dimensional response to one input direction; tomography is an analogy for recovering matrix information from directional measurements~\cite{radon1917bestimmung}. Because exact reconstruction of an arbitrary symmetric matrix requires $m \ge N$ linearly independent input directions, a practical optimizer with $m \in \{4, 8, 16, 32\}$ must choose which diagonal information to estimate:
\begin{quote}
\textit{Given a budget of $m \ll N$ directional projections, how should one place and orient of probe vectors $v_1, \dots, v_m$ to extract maximal actionable curvature?}
\end{quote}

Conventional approaches to this problem fall into two failure modes:
\begin{enumerate}
    \item \textbf{Isotropic Stochastic Probing (AdaHessian, Hutchinson):} These methods draw $v_k \sim \mathrm{Rademacher}(\pm 1)$ or $\mathcal{N}(0, I_N)$ and estimate the diagonal via $\hat{h}_i = \frac{1}{m} \sum_k v_i^k (H v^k)_i$~\cite{hutchinson1990stochastic,yao2021adahessian}. However, the estimator variance is dominated by off-diagonal cross-talk:
    \begin{equation}
    \mathrm{Var}(\hat{h}_i) = \frac{1}{m} \sum_{j \neq i} H_{ij}^2
    \end{equation}
    Its size depends on the current Hessian off-diagonal entries and need not decrease with the outer-loss gradient.
    \item \textbf{Other curvature approximations (Sophia, Shampoo):} Sophia-H~\cite{liu2023sophia} uses Hutchinson curvature estimates with clipped coordinate updates, while Shampoo~\cite{gupta2018shampoo} uses matrix preconditioners built from gradient statistics. Their computational costs and behavior differ from residual probing.
\end{enumerate}

\paragraph{Our Contribution: RADON.}
In this work, we introduce RADON, an optimizer and curvature estimation framework designed to resolve these fundamental barriers:
\begin{itemize}
    \item \textbf{Split-Exact Curvature Theory:} For a composite loss $\mathcal{L} = \ell \circ f$, the Hessian decomposes exactly as $H = S + R$. The structural core $S$ is positive semidefinite when the outer loss is convex and its diagonal admits single-backward Fisher sampling for cross-entropy. The residual $R$ is linear in the logit-space gradient and vanishes if that gradient vanishes.
    \item \textbf{Coded Tomographic Probing with Latin-Square Coloring:} Instead of probing $H$, we spend all $m$ directional probes on the residual $R = H - S$. By structuring probe vectors using Sylvester-Hadamard codes with Latin-square tensor coloring, intra-layer row and column interference is provably zero ($\bar{C}_{ij} = 0$).
    \item \textbf{Residual Probe Variance:} For an exact structural diagonal, the coded residual-probe variance scales quadratically when the outer-loss gradient is scaled. The implemented sampled structural diagonal has its own nonzero variance.
    \item \textbf{Machine-Checked Formal Proofs:} The stated carrier algebra, split, and generic coded-probe identities are checked in Lean 4 without project-specific axioms or \texttt{sorry}. The full neural optimizer and training outcomes are outside the formal model.
    \item \textbf{Planned Empirical Evaluation:} We provide experiment drivers for later FineWeb-Edu sweeps, competitive benchmarks, and ablations. No large-scale results are reported here.
\end{itemize}

\section{Mathematical Foundations: Carrier Algebra and the Split}

\subsection{Curvature Carriers and the Pullback Composition Law}

We formalize neural network modules as members of a category of curvature carriers.
\begin{definition}[Curvature Carrier]
For a smooth map $f: \mathbb{R}^n \to \mathbb{R}^m$, its curvature carrier at $x \in \mathbb{R}^n$ is the pair:
\begin{equation}
\mathcal{C}(f; x) = (J, \mathcal{H})
\end{equation}
where $J = Df(x) \in \mathbb{R}^{m \times n}$ is the Jacobian matrix, and $\mathcal{H}: \mathbb{R}^m \to \mathrm{Sym}(n)$ is a linear adjoint-curvature map defined by:
\begin{equation}
\mathcal{H}(\lambda) = \sum_{k=1}^m \lambda_k \nabla^2 f_k(x) = \nabla^2 \langle \lambda, f \rangle(x)
\end{equation}
\end{definition}

\begin{theorem}[Pullback Composition Law]
Let $f: \mathbb{R}^n \to \mathbb{R}^m$ and $g: \mathbb{R}^m \to \mathbb{R}^p$ be $C^2$ maps. The curvature carrier of the composite function $h = g \circ f$ satisfies:
\begin{equation}
J_h = J_g J_f, \quad \mathcal{H}_h(\lambda) = J_f^\top \mathcal{H}_g(\lambda) J_f + \mathcal{H}_f(J_g^\top \lambda) \quad (\star)
\end{equation}
Furthermore, the operation $\star$ is associative: $(\mathcal{C}_h \star \mathcal{C}_g) \star \mathcal{C}_f = \mathcal{C}_h \star (\mathcal{C}_g \star \mathcal{C}_f)$.
\end{theorem}
\begin{proof}
Machine-checked in Lean 4 (\texttt{Radon.comp\_assoc}, \texttt{Radon.chain\_snd}). The identity carrier $\mathrm{id}_{\mathcal{C}} = (I_n, 0)$ satisfies $\mathrm{id} \star \mathcal{C} = \mathcal{C} \star \mathrm{id} = \mathcal{C}$.
\end{proof}

\subsection{The Structural-Residual Split ($H = S + R$)}

Consider a neural network with representation map $f: \mathbb{R}^N \to \mathbb{R}^K$ (mapping parameters to final logits) and a scalar loss $\ell: \mathbb{R}^K \to \mathbb{R}$. The total objective is $\mathcal{L}(\theta) = (\ell \circ f)(\theta)$.
Evaluating the composition law $(\star)$ at $\lambda = 1$ yields:

\begin{theorem}[Split-Exact Decomposition]
The loss Hessian $H = \nabla^2 \mathcal{L}(\theta) \in \mathbb{R}^{N \times N}$ decomposes exactly into:
\begin{equation}
H = S + R
\end{equation}
where:
\begin{align}
S &= J_f^\top (\nabla^2 \ell) J_f \quad &\text{(Structural Core)} \\
R &= \mathcal{H}_f(\nabla \ell) \quad &\text{(Residual Curvature)}
\end{align}
\end{theorem}

\begin{proposition}[Properties of $S$ and $R$]
\label{prop:split_props}
The two components possess fundamentally asymmetric properties:
\begin{enumerate}
    \item \textbf{Positive Semidefiniteness of $S$:} If $\ell$ is convex, $S \succeq 0$, and $\mathrm{diag}(S)_i \ge 0$ for all $i \in \{1, \dots, N\}$.
    \item \textbf{Fisher Equivalence:} For mean cross-entropy over $B$ independent positions, $S$ is the Generalized Gauss-Newton matrix and conditional model Fisher on those inputs. If $\tilde{g}$ is the gradient of mean cross-entropy with independently sampled labels, then $\mathrm{diag}(S) = B\,\mathbb{E}_{\tilde{y} \sim p_\theta}[\tilde{g} \odot \tilde{g}]$, computable via a single backward pass (zero double backwards).
    \item \textbf{Gradient Linearity of $R$:} $R$ is linear in the loss gradient: $\mathcal{H}_f(c \nabla \ell) = c \mathcal{H}_f(\nabla \ell)$. Thus, $R \to 0$ as $\|\nabla \ell\| \to 0$.
\end{enumerate}
\end{proposition}
\begin{proof}
Machine-checked in Lean 4 (\texttt{Radon.core\_posCore}, \texttt{Radon.posCore\_diag\_nonneg}, \texttt{Radon.residual\_smul}).
\end{proof}

\section{Coded Tomographic Probing}

\subsection{The Master Probing Identity}

For any symmetric matrix $M \in \mathbb{R}^{N \times N}$ and sign probe $v \in \{-1, +1\}^N$, note that $v_i^2 = 1$. Multiplying the directional response $Mv$ by $v$ elementwise gives:
\begin{equation}
v_i (M v)_i = M_{ii} + \sum_{j \neq i} M_{ij} v_i v_j
\end{equation}
Over a cycle of $m$ probe vectors $v^1, \dots, v^m$, the time-averaged estimate is:
\begin{equation}
\hat{M}_{ii} = \frac{1}{m} \sum_{k=1}^m v_i^k (M v^k)_i = M_{ii} + \sum_{j \neq i} M_{ij} \bar{C}_{ij}
\end{equation}
where $\bar{C}_{ij} = \frac{1}{m} \sum_{k=1}^m v_i^k v_j^k \in [-1, 1]$ is the empirical cross-coordinate code coherence.

\subsection{Latin-Square Tensor Coloring}

In deep models, weight matrices $W \in \mathbb{R}^{d_{\text{out}} \times d_{\text{in}}}$ possess dominant coupling along rows and columns. We construct probe vectors using Sylvester-Hadamard matrices $H_m \in \{-1, +1\}^{m \times m}$ with $m = 2^k$.
\begin{definition}[Latin-Square Code Assignment]
For weight tensor element $W_{a, b}$ ($a \in [d_{\text{out}}], b \in [d_{\text{in}}]$), we assign the Hadamard row index:
\begin{equation}
\mathrm{code}(a, b) = (a + b) \pmod m
\end{equation}
\end{definition}

\begin{theorem}[Orthogonal Interference Cancellation]
Under Latin-square code assignment, any two distinct entries in the same row ($W_{a, b_1}$ and $W_{a, b_2}$) or same column ($W_{a_1, b}$ and $W_{a_2, b}$) with coordinate difference not divisible by $m$ receive orthogonal Hadamard codes:
\begin{equation}
\bar{C}_{(a, b_1), (a, b_2)} = 0, \quad \bar{C}_{(a_1, b), (a_2, b)} = 0
\end{equation}
\end{theorem}
\begin{proof}
The Lean theorem \texttt{Radon.orthogonal\_zero\_variance} proves zero variance when the required code orthogonality holds. The Latin-square assignment itself is checked by the implementation and small numerical tests, rather than by that theorem.
\end{proof}

\subsection{Random Global Sign-Flip Debiasing}

To ensure unbiasedness across all arbitrary non-adjacent pairs, we apply an independent global Rademacher sign flip $s \in \{-1, +1\}^N$ to the entire cycle: $v^k = s \odot c^k$.

\begin{theorem}[Exact Variance Factorization]
Under a uniform global sign flip, the cycle estimator $\hat{M}$ is strictly unbiased ($\mathbb{E}[\hat{M}_{ii}] = M_{ii}$), and its variance satisfies:
\begin{equation}
\mathrm{Var}(\hat{M}_{ii}) = \sum_{j \neq i} M_{ij}^2 \cdot \bar{C}_{ij}^2
\end{equation}
\end{theorem}
\begin{proof}
Machine-checked in Lean 4 (\texttt{Radon.coded\_unbiased}, \texttt{Radon.coded\_variance}). For one independent Hutchinson probe, $\bar{C}_{ij}^2 \le 1$ gives a no-worse ideal variance bound; this does not imply a universal improvement over $m$ independent probes (\texttt{Radon.coded\_le\_hutchinson}).
\end{proof}

\subsection{The Fusion Principle: Probing the Residual Alone}

The central operational doctrine of RADON is:
\begin{center}
\textit{Never spend directional probes on $H$. Take $S$ structurally, and probe $R = H - S$ alone.}
\end{center}

\begin{theorem}[Residual Variance with an Exact Structural Diagonal]
Let $\hat{h}_i = S_{ii} + \hat{R}_{ii}$, where $S_{ii}$ is exact and $\hat{R}_{ii}$ is obtained via $m$ coded probes on $R$. With the model derivatives fixed, scaling the outer-loss gradient by $c$ scales $R$ by $c$ and the probe variance by $c^2$:
\begin{equation}
\mathrm{Var}(\hat{h}_i) = \sum_{j \neq i} R_{ij}^2 \bar{C}_{ij}^2.
\end{equation}
In particular, the residual-probe variance is zero if the outer-loss gradient is zero.
\end{theorem}
\begin{proof}
Machine-checked in Lean 4 (\texttt{Radon.split\_variance\_residual\_only}, \texttt{Radon.split\_variance\_scaling}, \texttt{Radon.split\_exact\_at\_critical}). In the implementation the structural diagonal is sampled, so total estimator variance also includes structural sampling noise. A zero parameter gradient alone does not imply a zero outer-loss gradient.
\end{proof}

\section{The RADON Optimization Algorithm}

\begin{tcolorbox}[colback=gray!5,colframe=gray!50,arc=2mm,title={\textbf{Algorithm 1: RADON Optimizer}}]
\small
\textbf{Require:} Learning rate $\eta_t$, momentum $\beta_1$, EMA decays $\beta_s, \beta_r$, damping $\gamma$, probe period $k$, budget $m=16$.
\begin{enumerate}
    \item Initialize $\mu_0 = 0$, $\hat{s}_0 = 0$, $\hat{r}_0 = 0$, step $t = 0$.
    \item \textbf{For} each iteration $t = 1, 2, \dots$ \textbf{do}:
    \begin{enumerate}
        \item Compute stochastic mini-batch gradient $g_t = \nabla \mathcal{L}(\theta_t)$.
        \item Update momentum: $\mu_t = \beta_1 \mu_{t-1} + (1 - \beta_1) g_t$.
        \item \textbf{If} $t \pmod k == 0$ \textbf{then}:
        \begin{itemize}
            \item Compute sampled-label mean-loss gradient $\tilde{g}_t$ on a sub-batch of $B$ positions; update $\hat{s}_t = \beta_s \hat{s}_{t-1} + (1 - \beta_s) B(\tilde{g}_t \odot \tilde{g}_t)$.
            \item At the start of each code cycle, sample a sign flip $s \sim \{-1, +1\}^N$ and hold it fixed for all $m$ probes in that cycle.
            \item Compute cycle index $r = (t / k) \pmod m$, probe $v = s \odot c^{(r)}$ via Latin coloring.
            \item Evaluate residual product: $p = H v - S v = \mathrm{HVP}(v) - J_f^\top (\nabla^2 \ell) J_f v$.
            \item Accumulate $p \odot v$; update the residual EMA from the average only when the $m$-probe cycle completes.
        \end{itemize}
        \item \textbf{Else}: $\hat{s}_t = \hat{s}_{t-1}$, $\hat{r}_t = \hat{r}_{t-1}$.
        \item Fused curvature: $\hat{h}_t = \hat{s}_t + \hat{r}_t$.
        \item Preconditioned direction: $u_t = \mathrm{clamp}\left( \frac{\mu_t / (1 - \beta_1^t)}{\max(\gamma \hat{h}_t, \epsilon)}, -1, 1 \right)$.
        \item Parameter update: $\theta_{t+1} = \theta_t - \eta_t u_t - \eta_t \lambda_{\mathrm{wd}} \theta_t$.
    \end{enumerate}
\end{enumerate}
\end{tcolorbox}

\section{Empirical Evaluation Plan}

The planned Phase 6 sweep, Phase 7 competitive benchmark, and Phase 8 ablations have not been run to completion. CPU smoke tests exercise the experiment code on small models, but they cannot establish validation perplexity, hardware efficiency, or relative optimizer performance. Results and figures will be added only after measured runs with real FineWeb-Edu data, held-out validation, exact token accounting, and complete configuration records.

\section{Formal Verification in Lean 4}

The listed carrier and probe identities are machine-verified in Lean 4 and Mathlib. The formal development resides in \texttt{proofs/RadonCert/RadonCert/Radon.lean} and contains zero \texttt{sorry} placeholders and no project-specific axioms. Axiom inspection reports Lean's standard \texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound}:
\begin{itemize}
    \item \texttt{Radon.split\_exact}: Exactness of the $H = S + R$ decomposition.
    \item \texttt{Radon.core\_posCore}: Real positive semidefiniteness of $S$.
    \item \texttt{Radon.coded\_variance}: Exact formula for estimator variance under coded cycles.
    \item \texttt{Radon.orthogonal\_zero\_variance}: Strict zero variance on orthogonal coordinate pairs.
    \item \texttt{Radon.split\_variance\_residual\_only}: Proof for an exact structural diagonal; the sampled implementation has core-channel noise.
    \item \texttt{Radon.split\_variance\_scaling}: Exact scaling when the outer-loss gradient is multiplied by a scalar, with model derivatives fixed.
    \item \texttt{Radon.split\_exact\_at\_critical}: Exact residual recovery when the outer-loss gradient is zero and the structural diagonal is exact.
    \item \texttt{Radon.directional\_newton\_optimal}: Exact 1D Newton minimizer along any probe ray.
\end{itemize}

\section{Conclusion}

We have presented RADON, a coded probing approach to diagonal curvature estimation. The algebraic decomposition and probe identities motivate the implementation. Whether the optimizer improves training quality or efficiency at scale remains an empirical question for the planned FineWeb-Edu experiments.

\bibliographystyle{plain}
\bibliography{refs}

\end{document}
